<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <script src="https://cdn.tailwindcss.com"></script>
    <title>Document</title>
</head>
<body>
    
    <div class="w-full min-h-screen bg-zinc-900 text-white p-10">
        <div class="w-full flex justify-end ">
            <a class="bg-red-500 px-3 py-2 mb-5 inline-block text-sm rounded-md" href="/logout">Logout</a>
        </div>
        <h1 class="text-3xl mb-5">Hello, <%= user.name%> welcome. </h1>
        <h2 class="mb-5">You can create a post</h2>
        <form action="/post" method="post">
            <textarea placeholder="write something" class="p-3 outline-none w-1/3 resize-none bg-transparent border-zinc-800 border-2 rounded-md" name="content"></textarea>
            <input class="px-3 py-2 text-sm w-40 bg-blue-500 mt-2 block rounded-md outline-none" type="submit" value="create new post">
        </form>
        <div class="posts mt-20">
            <h3 class="text-zinc-400">You posts</h3>
           <div class="postcontainer mt-5">
            
            <% user.posts.forEach(function(post) { %>
                <div class="post w-1/3 border-2 mb-4 border-zinc-700 bg-zinc-800 p-4">
                    <h4 class="text-blue-500 mb-4">@<%= user.username %>></h4>
                    <p class="tracking-tight text-sm"><%= post.content %></p>
                    <small class="mt-2 inline-block"><%= post.likes.length %> Likes</small>
                    <div class="btn flex gap-4 mt-1">
                        <a class="text-blue-500" href="/like/<%= user._id %>">
                            <%= post.likes.indexOf(user.id)===-1 ? "Like":"Unlike" %>
                        </a>
                        <a class="text-zinc-400" href="/edit/<%= post._id%>">Edit</a>
                        <!-- console.log(post._id); -->
                    </div>
                </div>
            <% }); %>
            
        </div>
        
    </div>
</body>
</html>