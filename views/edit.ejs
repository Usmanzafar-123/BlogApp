<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <script src="https://cdn.tailwindcss.com"></script>
    <title>Document</title>
  </head>
  <body>
    <div class="w-full min-h-screen bg-zinc-900 text-white p-10">
      <div class="w-full flex justify-end">
        <a
          class="bg-red-500 px-3 py-2 mb-5 inline-block text-sm rounded-md"
          href="/logout"
          >Logout</a
        >
      </div>

      <h2 class="mb-5">You can edit your post</h2>
      <form action="/update/<%= post._id%>" method="post" >
        <textarea
          placeholder="write something"
          class="p-3 outline-none w-1/3 resize-none bg-transparent border-zinc-800 border-2 rounded-md h-{150}"
          name="content"
        ><%= post.content%></textarea>
        <input
          class="px-3 py-2 text-sm w-40 bg-yellow-500 mt-2 block text-black rounded-md outline-none"
          type="submit"
          value="update"
        />
      </form>
    </div>
  </body>
</html>
